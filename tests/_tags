true: warn(+a-40-42-44-45-70)

# use custom buid to prevent dynamic linking to system wide installed stubs
true: custom

<*.ml>: use_lmdb

<*.{byte,native}>: use_lmdb

<bench.{ml,byte,native}>: package(benchmark)

<{pr,test}.*>: thread, package(alcotest), package(fmt)
