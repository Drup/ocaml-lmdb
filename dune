(executable
 (name discover)
 (modules discover)
 (libraries str dune.configurator))

(subdir config
 (rule
  (alias config)
  (deps ../src/stubs_shipped.inc ../src/stubs_system.inc)
  (targets cflags.sexp clibs.sexp stubs.inc)
  (action (run ../discover.exe)))
)
